#!/usr/bin/node
// a string that prints 3 lines but using an array of a string and aloop
const fide = ["C is fun",
"Python is cool",
"JavaScript is amazing"];
for (let i = 0; i < 3; i++){
    console.log(fide[i]);
}